# FMOD Importer Tool - Python Dependencies
#
# Note: This script uses mainly standard Python libraries
# that are normally already included in a standard Python installation.
#
# If you encounter problems, install with:
#   pip install -r requirements.txt

# No external dependencies required!
# All libraries used are standard:
# - tkinter (GUI - included in Python on Windows and macOS)
# - xml.etree.ElementTree (XML parsing - standard)
# - json (JSON handling - standard)
# - pathlib (path handling - standard)
# - subprocess (process execution - standard)
# - wave (WAV file reading - standard)

# For Linux/Ubuntu, if tkinter is not available:
# sudo apt-get install python3-tk

# To verify everything works:
# python -c "import tkinter; import xml.etree.ElementTree; import json; print('OK')"
